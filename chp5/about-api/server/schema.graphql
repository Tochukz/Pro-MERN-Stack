type Issue {
  id: Int!
  title: String!
  status: String!
  owner: String!
  effor: Int
  created: String!
  due: String
}

#### Top level declarations

type Query {
  about: String!
  issueList: [Issue!]!
}

type Mutation {
  setAboutMessage(message: String!): String
}
